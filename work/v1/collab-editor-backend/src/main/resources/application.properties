server.port=8080
spring.application.name=collab-editor

# CORS Configuration
cors.allowed-origins=http://localhost:3000

# WebSocket Configuration
websocket.endpoint=/ws
websocket.destination-prefix=/app
websocket.broker-prefix=/topic,/queue

# Logging
logging.level.com.collab.editor=DEBUG
